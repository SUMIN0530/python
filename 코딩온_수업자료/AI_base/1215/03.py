# 미분
# 순간적인 변화율

# 일상 예시
# 자동차 속도계
# - 현재 속도 = 위치의 순간 변화율

# 평균 변화율 vs 순간 변화율
# 평균 변화율
# 서울 -> 부산 400km, 4시간 소요
# 평균 속도 = 100km / hour

# 순간 변화율(미분)
# - 특정 순간의 속도
# - 속도계가 보여주는 값
# - 시간 간격을 극한으로 줄임

# 기하학적 의미
# 미분 = 그래프 접선 기울기

# y = x² 그래프에서
# x = 2에서 접선의 기울기 = 4   ==> f'(x) = 기울기 ㅋㅋㅋ... 
# x = 0에서 접선의 기울기 = 0
# x = -2에서 접선의 기울기 = -4

# 기본 미분 공식
# 미분 표기법
# y = f(x)일 때, 미분을 표헌하는 방법
# f'(x) <- 프라임 표기
# dy / dx <- 라이프니트 표기
# d / dx f(x) <- 미분 연산자

# 기본 공식
# f(x) = 5
# f'(x) = 0 <---  기울기

# f(x) = 5x
# f'(x) = 5

# 거듭제곱 미분 
# f(x) = x^n
# f'(x) = n * x^(n-1)

# f(x) = 5x²
# f'(x) = 10

# 덧셈 법칙
# (f + g)' = f' + g'
# f(x) = x² + x
# f'(x) = 2x + 1

# # 상수배 법칙
# (c X f)' = c X f'
# f(x) = 3x²
# f'(x) = 6x

# 편미분
# 여러 변수가 있을 때, 변수 하나만 미분 (나머지 고정)
# f(x, y) = x² + y²

# x에 대한 편미분 (y 상수 취급)
# f'(x, y) = 2x + y² 

# y에 대한 편미분
# f'(x, y) = x² + 2y

# 따오기 (파샬)

# AI에서 미분의 역할
# 손실 함수와 미분
# AI 학습의 핵심
# 손실(오차)을 최소화하는 방향을 가중치를 조정

# 손실 함수를 가중치로 미분
# - 손실이 감소하는 방향을 알 수 있음
# - 그 방향으로 가중치 업데이트

# 그래디언트
# 모든 편미분을 벡터로 모은 것
# f(x, y) = x² + y²일 때
# 그래디언트 따오기

# (3, 4)에서 그래디언트 = [6, 8]
# 의미
# 함수가 가장 급격히 증가하는 방향
# (반대방향) => 가장 급격히 감소하는 방향

# 경사 하강법
# 현재 위치에서 가장 경사가 급한 방향 확인
# 그 방향으로 한 걸음 이동
# 반복 -> 결국 가장 낮은 곳에 도착

# AI
# 현재 손실에서 그래디언트 계산
# 손실이 줄어드는 방향으로 가중치를 조정
# 반복 -> 최소 손실에 도달

# 수학적 표현
# 목표 : f(x)를 최소로 만드는 x 찾기

# 경사 하강법
# x_new = x_old - 따오기

# 따오기





# 연쇄 법칙




# ================ 실습 ==================
